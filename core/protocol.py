# Protocol
#
# The class definition of a single protocol.
# Note that this is *NOT* a whole workflow. Just one self-contained step.

class Protocol:
  def __init__(self, namestring):
    self._name = namestring
