# Hash
#
# Utilities for generating unique hashes for files and directories

import os

def file_hash(file):
  pass

#def directory_hash(dir):
#  for (dirpath, dirnames, filenames) in os.walk(dir):
#    print filenames
#    statinfo = os.stat(
